;; This file might delete later
(defn defend_self[](
       (def qs "delete from some where tag='players'")
       (def qs_done "insert into person (flavour, past_flavour) values('Lime', 'Lime Crush')"
       (exec_my_db qs)
       (exec_my_db qs_done)
         ))
  
  (defend_self)
  
  ;; Soon recover 
  
         
